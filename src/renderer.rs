use crate::creature::Creature;

pub struct Renderer {
    // Рендерер будет реализован позже
}

impl Renderer {
    pub fn new() -> Self {
        Self {}
    }

    pub fn render_creature(&self, _creature: &Creature) {
        // Реализация отрисовки существ будет добавлена позже
    }
}


